# 弹幕回声（Danmaku Echo）隐私权政策

生效日期：2026 年 8 月 24 日

弹幕回声是一款用于增强虎牙直播、哔哩哔哩直播、斗鱼直播和抖音直播弹幕交互的浏览器扩展。本政策说明扩展如何处理、保存和共享用户数据。

## 处理的数据

为了提供弹幕 `+1`、快捷回复、本地收藏、高频 +1 提醒、全屏交互和平台独立设置，扩展会在受支持的直播页面中处理以下数据：

- 直播页面中的弹幕和聊天内容，包括文字、Unicode Emoji、平台图片表情及相关页面元素；
- 弹幕发送者的公开显示名称或用户标识，用于生成快捷回复中的 `@发送者`；
- 当前直播平台、直播间标识和直播间名称，用于区分本房收藏与其他直播间收藏；
- 用户主动触发扩展功能时的交互数据，例如点击操作、快捷键、收藏发送次数和最近使用时间；
- 扩展功能开关、平台颜色及其他用户设置。
- 高频 +1 提醒会在当前页面内存中临时记录最近一分钟普通文字弹幕的规范化文本、来源和出现时间。

扩展不会读取密码、支付信息、健康信息或精确地理位置。仅当用户主动发送哔哩哔哩房间图片表情、且官方表情面板无法唯一定位对应资源时，一次性后备发送函数会在当前哔哩哔哩页面内读取 CSRF Cookie 并立即提交给哔哩哔哩；该凭据不会离开页面上下文、返回扩展后台、写入日志或保存到存储中。

## 数据用途

上述数据只用于提供扩展公开说明中的直播弹幕交互功能：识别并显示弹幕操作、复读用户选中的弹幕、填写快捷回复、管理和排序本地收藏、判断相同文字是否达到 +1 提醒门槛，以及应用用户设置。高频提醒不生成热词、话题、问题、时间线或摘要，不调用本地模型或云端分析接口。

当用户主动点击 `+1` 或发送收藏时，所选弹幕内容优先通过对应直播平台页面已有的官方输入框和发送流程提交给该平台。哔哩哔哩房间图片表情在官方面板无法唯一定位时，可通过当前页面的直播弹幕接口后备发送；发送前会验证 `room_<房间>_<资源>` 标识所属真实房间与当前直播间一致，并生成当前请求所需签名。扩展不会绕过平台的登录、表情权限、审核、限流或其他规则。

为了把平台限流、重复发送和审核结果准确显示在扩展提示中，用户主动发送后会在当前页面短暂运行一次性发送观察器。观察器最多运行 8 秒，只向扩展返回请求方法、去除查询参数后的接口路径、HTTP 状态、平台业务码和最长 180 个字符的官方错误文案；请求头、请求体、Cookie、CSRF、签名和完整 URL 不会离开页面上下文。斗鱼和虎牙以 WebSocket 发送时，仅在可可靠识别为弹幕请求时返回脱敏的通道摘要，不能读取业务回包时仍以官方页面提示为准。

## 数据存储与保留

- 扩展设置保存在 `chrome.storage.sync`。如果用户启用了 Chrome 同步，这些设置可能由 Google Chrome 按用户的同步配置在其设备之间同步；扩展开发者无法访问这些同步数据。
- 收藏的弹幕内容（包括文字、Unicode Emoji、平台图片表情的资源地址与识别信息、内容顺序）、来源平台、直播间标识和名称、收藏时间、发送次数及最近使用时间保存在 `chrome.storage.local`，不会由扩展上传到开发者或第三方服务器。
- 用户点击弹幕雷达首次说明中的“知道了”或“进入设置”后，扩展会在 `chrome.storage.local` 保存一个布尔确认标记，避免重复显示说明框；该标记不包含弹幕或账号信息。
- 高频 +1 提醒的最近一分钟计数只存在于当前直播页面内存中，不写入 `storage.local`、`storage.session` 或同步存储；页面关闭或刷新后立即消失。
- 为识别弹幕和快捷回复而临时处理的发送者信息、页面内容和指针位置只在当前页面运行期间使用，不会发送到开发者服务器。

用户可以在扩展的收藏面板中删除单条收藏，也可以通过 Chrome 的扩展数据管理功能或卸载扩展来删除全部本地数据。

## 数据共享与传输

扩展不运营数据收集服务器，不包含分析、广告或跟踪服务，也不会出售用户数据。

除用户主动要求通过直播平台官方发送流程提交弹幕之外，扩展不会向第三方传输数据。扩展不会将数据用于与直播弹幕交互无关的目的，也不会将数据用于广告、信用评估或贷款用途。

扩展设置可能按照用户的 Chrome 同步配置由 Google Chrome 同步。此同步由 Chrome 提供并受 Google 相关隐私政策约束。

## 远程代码

扩展不下载或执行远程 JavaScript、WebAssembly、模型或其他分析代码。全部可执行代码均包含在安装的软件包中。设置页中的帮助和项目主页链接只会在用户点击时打开普通网页，不会把远程网页代码作为扩展代码执行。

## 权限用途

- `storage`：保存扩展设置、本地弹幕收藏和弹幕雷达首次说明的确认标记。
- `scripting`：在抖音通过单页应用路由进入直播间时注入扩展软件包内的直播功能脚本和样式；执行 B 站房间图片表情一次性后备发送；在用户主动发送后执行最多 8 秒的一次性原生发送观察器。
- 直播网站访问权限：仅在虎牙、哔哩哔哩、斗鱼和抖音直播页面中识别弹幕、显示交互按钮、操作官方输入框并读取脱敏后的发送结果。普通抖音页面只运行用于判断是否进入直播路由的轻量启动器，不读取无关页面内容。

## Chrome Web Store Limited Use

扩展对用户数据的使用仅限于提供和改进其公开说明中的单一用途及面向用户的功能。扩展不会将用户数据用于个性化广告，不会出售用户数据，也不会允许开发者或其他人员读取用户数据，法律要求或用户为获得支持而明确授权的情形除外。

## 政策更新与联系

如果扩展的数据处理方式发生变化，本政策将同步更新，并在适用时通过扩展界面或产品说明向用户披露。

如对本政策有疑问，可以通过以下邮箱或项目的 GitHub Issues 页面联系开发者：

sadunicorn1113@gmail.com

https://github.com/SadUnicorn171/danmaku-echo/issues
